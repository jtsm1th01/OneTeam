<%= form_for(@employee) do |f| %>
  <% if @employee.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@employee.errors.count, "error") %> prohibited this employee from being saved:</h2>

      <ul>
      <% @employee.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :employee_name %><br>
    <%= f.text_field :employee_name %>
  </div>
  <div class="field">
    <%= f.label :employee_email %><br>
    <%= f.text_field :employee_email %>
  </div>
  <div class="field">
    <%= f.label :years_with_company %><br>
    <%= f.number_field :years_with_company %>
  </div>
  <div class="field">
    <%= f.label :title_id %><br>
    <%= f.collection_select(:title_id, @titles, :id, :title ) %>
  </div>
  <div class="field">
    <%= f.label :group_id %><br>
    <%= f.collection_select(:group_id, @groups, :id, :group_name ) %>
  </div>
  <div class="field">
    <%= f.label :location_id %><br>
    <%= f.collection_select(:location_id, @locations, :id, :location_name ) %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
