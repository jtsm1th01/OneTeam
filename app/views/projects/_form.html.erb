<%= form_for(@project) do |f| %>
  <% if @project.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@project.errors.count, "error") %> prohibited this project from being saved:</h2>

      <ul>
      <% @project.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :ProjName %><br>
    <%= f.text_field :ProjName %>
  </div>
  <div class="field">
    <%= f.label :Contact %><br>
    <%= f.number_field :Contact %>
  </div>
  <div class="field">
    <%= f.label :ProjStart %><br>
    <%= f.date_select :ProjStart %>
  </div>
  <div class="field">
    <%= f.label :ProjEnd %><br>
    <%= f.date_select :ProjEnd %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
